name: Bump version and release
on:
  pull_request:
    types:
      - closed
    branches:
      - main
jobs:
  tag:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{steps.tag.outputs.new_tag}}
    steps:
    - uses: actions/checkout@v3
      with:
        ref: ${{ github.sha }}
        fetch-depth: '0'

    - name: Bump version and push tag
      id: tag
      uses: anothrNick/github-tag-action@1.61.0 # Don't use @master unless you're happy to test the latest version
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        DEFAULT_BUMP: patch
        WITH_V: true

  release:
    runs-on: ubuntu-latest
    needs: [tag]
    steps:
      - name: Set up Go 1.x
        uses: actions/setup-go@v2
        with:
          go-version: ^1.20.2

      - name: Check out code into the Go module directory
        uses: actions/checkout@v3

      - name: Build
        run: |
          mkdir release
          cd sqlite
          go generate
          mv s3db.so ../release/s3db-${{ needs.tag.outputs.tag }}-linux-amd64.sqlite-ext.so
          cd ../release
          gzip -9 s3db-${{ needs.tag.outputs.tag }}-linux-amd64.sqlite-ext.so

      - name: Create Release
        id: create_release
        uses: "marvinpinto/action-automatic-releases@v1.2.1"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: ${{ needs.tag.outputs.tag }}
          prerelease: false
          files: |
            release/s3db-${{ needs.tag.outputs.tag }}-linux-amd64.sqlite-ext.so.gz

